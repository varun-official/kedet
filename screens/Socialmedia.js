import React, {useEffect, useState} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
  Image,
  TouchableOpacity,
  Animated,
} from 'react-native';

import Icon from 'react-native-vector-icons/Ionicons';

const Socialmedia = () => {
  return (
    <View>
      <Text>it is Socialmedia 🚀</Text>
    </View>
  );
};

export default Socialmedia;

const styles = StyleSheet.create({});
